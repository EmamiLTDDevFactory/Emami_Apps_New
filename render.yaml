services:
  - type: web
    name: emami-apps-hub
    runtime: node
    plan: free
    buildCommand: npm install && npm run build:web
    startCommand: npm run serve
    envVars:
      - key: NODE_VERSION
        value: 20.18.0
